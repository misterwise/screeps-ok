{
	"expected_failures": {
		"tombstone-corpse-rate": {
			"actual": "No body energy/mineral reclaim at all on death — `mods/creep/tombstone.ts` buryCreep only multiplies carried store by rate, never iterates the body for part-cost or boost resources",
			"expected": "On death, tombstone store includes reclaimed body resources (`BODYPART_COST[type] * lifeRate`, boost minerals at `LAB_BOOST_MINERAL * lifeRate`). CLAIM bodies use `CREEP_CLAIM_LIFE_TIME` as the life-rate denominator; other bodies use `CREEP_LIFE_TIME`",
			"tests": ["CREEP-DEATH-009", "CREEP-DEATH-010"]
		},
		"death-carried-store-rate-multiplied": {
			"actual": "`buryCreep` multiplies carried store by `rate` (`Math.floor(amount * CREEP_CORPSE_RATE)`), so a creep carrying 50 energy leaves only 10 in the tombstone",
			"expected": "Carried resources are deposited 1:1 regardless of `dropRate` (vanilla `_die.js:75-91` iterates `object.store` without the rate multiplier — only body-reclaim resources are scaled by `lifeRate`)",
			"tests": ["CREEP-DEATH-004", "CREEP-DEATH-008"]
		},
		"death-container-diversion": {
			"actual": "Creep death never diverts into a same-tile container — `buryCreep` writes directly to the tombstone. Mixed-resource deaths that should sequentially fill the container (body-energy, then body-minerals, then carried store) leave the container untouched",
			"expected": "Death resources flow into a live same-tile container before any tombstone remainder, with each resource type tried against the container's current free capacity in vanilla's deposit order",
			"tests": ["CREEP-DEATH-003", "CREEP-DEATH-012"]
		},
		"factory-not-owner-precedence": {
			"actual": "`factory.produce` on unowned factory returns `ERR_RCL_NOT_ENOUGH`",
			"expected": "Returns `ERR_NOT_OWNER` before any RCL check",
			"tests": ["FACTORY-PRODUCE-010"]
		},
		"rampart-no-protection": {
			"actual": "Ramparts do not absorb damage for objects on their tile (no rampart redirect in combat/dismantle processors)",
			"expected": "Damage targeting an object on a rampart tile is redirected to the rampart",
			"tests": ["RAMPART-PROTECT-001", "RAMPART-PROTECT-002", "DISMANTLE-004", "COMBAT-MELEE-005", "COMBAT-RANGED-006", "COMBAT-RMA-004"]
		},
		"renew-while-spawning": {
			"actual": "`Spawn.renewCreep` returns `OK` while spawn is actively spawning",
			"expected": "Returns `ERR_BUSY` while spawn is actively spawning",
			"tests": ["RENEW-CREEP-009"]
		},
		"renew-rejects-boosted-creep": {
			"actual": "`Spawn.renewCreep` rejects boosted creeps with `ERR_NO_BODYPART`",
			"expected": "Accepts boosted creeps and strips the boosts during renew",
			"tests": ["RENEW-CREEP-004", "RENEW-CREEP-005", "RENEW-CREEP-006"]
		},
		"recycle-no-body-reclaim": {
			"actual": "`Spawn.recycleCreep` only kills the creep (processor marked TODO); no energy deposited",
			"expected": "Deposits `floor(bodyCost × ttlRemaining / CREEP_LIFE_TIME)` energy into the tombstone via `_die` with `dropRate=1.0`",
			"tests": ["RECYCLE-CREEP-002"]
		},
		"lab-not-owner-precedence": {
			"actual": "Lab action on unowned lab returns `ERR_RCL_NOT_ENOUGH`",
			"expected": "Returns `ERR_NOT_OWNER` before any RCL check",
			"tests": ["LAB-RUN-012", "LAB-REVERSE-012"]
		},
		"observer-not-owner-precedence": {
			"actual": "`observeRoom` on unowned observer returns `ERR_RCL_NOT_ENOUGH`",
			"expected": "Returns `ERR_NOT_OWNER` before any RCL check",
			"tests": ["OBSERVER-006"]
		},
		"safemode-concurrent-allowed": {
			"actual": "Allows `activateSafeMode` on multiple owned controllers simultaneously",
			"expected": "Returns `ERR_BUSY` when any owned controller already has an active safe mode",
			"tests": ["CTRL-SAFEMODE-007"]
		},
		"container-destroy-no-spill": {
			"actual": "Container destroyed by decay does not drop its contents as ground resources",
			"expected": "Container contents become dropped resources on the tile when decay destroys it",
			"tests": ["CONTAINER-002"]
		},
		"destroy-ownership-bypass": {
			"actual": "`structure.destroy()` allowed when room controller not owned by player",
			"expected": "Returns `ERR_NOT_OWNER` when the room controller is missing or not owned by the player",
			"tests": ["STRUCTURE-API-001"]
		},
		"notifyWhenAttacked-not-implemented": {
			"actual": "`structure.notifyWhenAttacked()` not implemented",
			"expected": "Enforces `ERR_NOT_OWNER`/`ERR_INVALID_ARGS`, returns `OK` on success, and persists the setting next tick",
			"tests": ["STRUCTURE-API-004", "STRUCTURE-API-005", "STRUCTURE-API-006", "STRUCTURE-API-007", "STRUCTURE-API-008"]
		},
		"eventlog-attack-missing": {
			"actual": "`getEventLog()` missing `EVENT_ATTACK` entries for combat",
			"expected": "`EVENT_ATTACK` entries appear in the event log for each combat hit",
			"tests": ["ROOM-EVENTLOG-001", "ROOM-EVENTLOG-002"]
		},
		"boost-energy-cost-scales": {
			"actual": "Energy cost scales with boost multiplier for repair/upgrade",
			"expected": "Boosted repair/upgrade consume the same unboosted energy amount per work part",
			"tests": ["BOOST-BUILD-002", "BOOST-UPGRADE-002"]
		},
		"withdraw-enemy-rampart-no-protection": {
			"actual": "`withdraw()` does not enforce `ERR_NOT_OWNER` for hostile structures under a non-public enemy rampart",
			"expected": "Returns `ERR_NOT_OWNER` when the target sits under a non-public hostile rampart",
			"tests": ["WITHDRAW-005"]
		},
		"generate-safe-mode-requires-work": {
			"actual": "`generateSafeMode()` requires a `WORK` body part (checkCommon guard)",
			"expected": "No body-part requirement; needs only `SAFE_MODE_COST` ghodium and range",
			"tests": ["CTRL-GENSAFE-001", "CTRL-GENSAFE-002", "CTRL-GENSAFE-003", "CTRL-GENSAFE-004"]
		},
		"controller-my-previously-owned-returns-undefined": {
			"actual": "`StructureController.my` returns `undefined` on a previously-owned controller (after `unclaim()` or downgrade-to-zero, where xxscreeps sets `#user` to `null` and the getter maps `null` → `undefined`)",
			"expected": "Returns `false` on a previously-owned controller (vanilla maps `user === null` → `false`, reserving `undefined` for never-owned)",
			"tests": ["CTRL-UNCLAIM-001", "CTRL-DOWNGRADE-002"]
		},
		"safemode-ignores-downgrade-threshold": {
			"actual": "`activateSafeMode` returns OK when `ticksToDowngrade` is below `CONTROLLER_DOWNGRADE_SAFEMODE_THRESHOLD`",
			"expected": "Returns `ERR_TIRED` when the downgrade timer is below the safe-mode threshold",
			"tests": ["CTRL-SAFEMODE-005"]
		},
		"tombstone-store-missing": {
			"actual": "Tombstone snapshot does not include store energy from combat kills",
			"expected": "Tombstone `store` includes carried resources plus body-part corpse energy",
			"tests": ["TOMBSTONE-003"]
		},
		"moveto-nopathfinding-returns-ok": {
			"actual": "`moveTo({noPathFinding: true})` returns `OK` when no cached path exists",
			"expected": "Returns `ERR_NOT_FOUND` when no reusable path is available",
			"tests": ["MOVE-BASIC-019"]
		},
		"pull-spawning-no-guard": {
			"actual": "`pull()` on a spawning creep returns `OK` (no spawning check in pull intent)",
			"expected": "Returns `ERR_INVALID_TARGET` when the target is a spawning creep",
			"tests": ["MOVE-PULL-007:spawning"]
		},
		"findpath-same-pos-not-empty": {
			"actual": "`Room.findPath()` returns a 1-step path when source equals destination",
			"expected": "Returns an empty result (`[]` or `''` with `serialize: true`) when source and destination match",
			"tests": ["LEGACY-PATH-006"]
		},
		"transfer-controller-no-upgrade-redirect": {
			"actual": "`transfer(controller, RESOURCE_ENERGY)` returns `ERR_NOT_IN_RANGE` and does not redirect",
			"expected": "Redirects to `upgradeController` behavior (or equivalent successful upgrade intent)",
			"tests": ["TRANSFER-011"]
		},
		"dismantle-no-destroy-at-zero-hits": {
			"actual": "`dismantle()` reducing hits to `0` leaves the structure present with `hits=0`",
			"expected": "Structure is destroyed in the same tick once hits reach `0`",
			"tests": ["DISMANTLE-007"]
		},
		"shape-extra-hits-my": {
			"actual": "Base RoomObject exposes `hits`/`hitsMax`/`my` on non-structure objects and leaks `my` onto unowned structures; wall missing `ticksToLive`; ruin missing `structureType`",
			"expected": "Non-structure objects omit `hits`/`hitsMax`/`my`; unowned structures omit `my`; wall exposes `ticksToLive`; ruin exposes `structureType`",
			"tests": ["SHAPE-SOURCE-001", "SHAPE-MINERAL-001", "SHAPE-SITE-001", "SHAPE-RESOURCE-001", "SHAPE-TOMBSTONE-001", "SHAPE-RUIN-001", "SHAPE-STRUCT-001:road", "SHAPE-STRUCT-001:constructedWall", "SHAPE-STRUCT-001:container"]
		},
		"shape-struct-missing-legacy-compat": {
			"actual": "Structure missing legacy compat getter: `storage.storeCapacity`",
			"expected": "Legacy compat getter is present on `storage`",
			"tests": ["SHAPE-STRUCT-001:storage"]
		},
		"shape-body-part-always-has-boost": {
			"actual": "Unboosted body parts expose `boost` key (with `undefined` value)",
			"expected": "`boost` key is only present when the part is actually boosted",
			"tests": ["SHAPE-CREEP-002", "SHAPE-CREEP-003"]
		},
		"shape-room-missing-survivalInfo": {
			"actual": "`Room` object missing `survivalInfo` property",
			"expected": "`Room.survivalInfo` is present (per canonical room shape)",
			"tests": ["SHAPE-ROOM-001"]
		},
		"shape-game-surface-mismatch": {
			"actual": "`Game` missing `cpuLimit`; `Game.flags` and `Game.powerCreeps` present but with different own-property surfaces than vanilla",
			"expected": "`Game` exposes canonical top-level fields with matching data-property surface",
			"tests": ["SHAPE-GAME-001"]
		},
		"shape-flag-crash": {
			"actual": "Flag shape discovery crashes (`Cannot use 'in' operator on undefined`)",
			"expected": "Flag objects expose the documented shape without crashing",
			"tests": ["SHAPE-FLAG-001"]
		},
		"spawn-duplicate-name-allowed": {
			"actual": "`spawnCreep` allows a name that collides with a currently spawning creep (no check against spawning creeps in `checkSpawn`)",
			"expected": "Returns `ERR_NAME_EXISTS` when the name collides with a spawning creep",
			"tests": ["SPAWN-CREATE-003"]
		},
		"rawmemory-set-no-eager-limit-check": {
			"actual": "`RawMemory.set(largeString)` returns normally; the 2MB cap throws later inside `memory/memory.ts:flush()` during `runtimeConnector.send`, surfaced to the adapter as a runtime sandbox error rather than a user-code exception",
			"expected": "`RawMemory.set` throws synchronously at call time when the value exceeds the 2MB limit, so a user-code try/catch can observe the throw",
			"tests": ["MEMORY-004"]
		},
		"rawmemory-set-invalidates-parsed-memhack": {
			"actual": "`RawMemory.set` clears the cached parsed `Memory`, so a subsequent `Memory.x` access re-parses from the newly-set string and loses pre-set mutations",
			"expected": "Setting `RawMemory` after `Memory` has been accessed preserves the already-parsed `Memory` object for the rest of the tick (memhack)",
			"tests": ["MEMORY-002"]
		},
		"foreign-segment-not-supported": {
			"actual": "`RawMemory.foreignSegment` is unpopulated; `setDefaultPublicSegment` is a no-op console.error; cross-user segment reads return `null`",
			"expected": "Foreign segment requests populate `RawMemory.foreignSegment` with `{ username, id, data }` on the following tick",
			"tests": ["RAWMEMORY-FOREIGN-002", "RAWMEMORY-FOREIGN-003", "RAWMEMORY-FOREIGN-004", "RAWMEMORY-FOREIGN-005", "RAWMEMORY-FOREIGN-006", "RAWMEMORY-FOREIGN-008"]
		},
		"packedpos-write-ignored": {
			"actual": "Writing to `RoomPosition.__packedPos` does not update the position's `x`, `y`, or `roomName` getters (likely stored separately from `__packedPos`)",
			"expected": "Assigning `__packedPos` updates `x`, `y`, `roomName` to the decoded values — bots use this to construct positions cheaply (including from WASM bridges)",
			"tests": ["UNDOC-PACKEDPOS-003"]
		},
		"memory-parsed-json-not-refreshed-across-ticks": {
			"actual": "xxscreeps caches the parsed-memory `json` object as module-level state (`mods/memory/memory.ts`) and does NOT re-parse raw memory at the start of each tick. Tick-end serialization correctly produces vanilla-compatible raw memory (function keys dropped, `NaN`/`Infinity` → `null` via `JSON.stringify`) but the in-memory `Memory` object on the next tick still contains the original values (the function object, `NaN`, `Infinity`) because it's the same cached `json` reference, not a fresh parse of the raw string.",
			"expected": "`Memory` on each tick reflects a fresh `JSON.parse(RawMemory.get())` — values that `JSON.stringify` coerces (functions stripped, `NaN`/`Infinity` → `null`) round-trip to those coerced forms when read on the next tick, matching vanilla's per-tick-re-parse semantics.",
			"tests": ["UNDOC-MEMJSON-001", "UNDOC-MEMJSON-003", "UNDOC-MEMJSON-004"]
		},
		"memory-circular-ref-crash": {
			"actual": "A circular reference in `Memory` causes xxscreeps's `crunch` normalizer (`mods/memory/memory.ts`) to recurse until stack overflow (`RangeError: Maximum call stack size exceeded`), crashing the player runtime. `crunch` has no cycle detection; the subsequent `JSON.stringify` would also throw, but `crunch` runs first and its throw is not caught.",
			"expected": "Circular references fail gracefully — the unserializable subtree does not persist, but the player runtime stays alive and other Memory keys that do not participate in the cycle remain readable on the next tick.",
			"tests": ["UNDOC-MEMJSON-005"]
		}
	}
}
