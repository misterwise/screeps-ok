{
	"expected_failures": {
		"rampart-no-protection": {
			"actual": "Ramparts do not absorb damage for objects on their tile (no rampart redirect in combat/dismantle processors)",
			"expected": "Damage targeting an object on a rampart tile is redirected to the rampart",
			"tests": ["RAMPART-PROTECT-001", "RAMPART-PROTECT-002", "DISMANTLE-004", "COMBAT-MELEE-005", "COMBAT-RANGED-006", "COMBAT-RMA-004"]
		},
		"renew-while-spawning": {
			"actual": "`Spawn.renewCreep` returns `OK` while spawn is actively spawning",
			"expected": "Returns `ERR_BUSY` while spawn is actively spawning",
			"tests": ["RENEW-CREEP-009"]
		},
		"renew-rejects-boosted-creep": {
			"actual": "`Spawn.renewCreep` rejects boosted creeps with `ERR_NO_BODYPART`",
			"expected": "Accepts boosted creeps and strips the boosts during renew",
			"tests": ["RENEW-CREEP-004", "RENEW-CREEP-005", "RENEW-CREEP-006"]
		},
		"recycle-no-body-reclaim": {
			"actual": "`Spawn.recycleCreep` only kills the creep (processor marked TODO); no energy deposited",
			"expected": "Deposits `floor(bodyCost × ttlRemaining / CREEP_LIFE_TIME)` energy into the tombstone via `_die` with `dropRate=1.0`",
			"tests": ["RECYCLE-CREEP-002"]
		},
		"eventlog-attack-missing": {
			"actual": "Several upstream event-log emission sites (PR #107) are not yet in the pin: EVENT_ATTACK, EVENT_OBJECT_DESTROYED, EVENT_TRANSFER, EVENT_EXIT, EVENT_ATTACK_CONTROLLER, EVENT_RESERVE_CONTROLLER, EVENT_UPGRADE_CONTROLLER",
			"expected": "All canonical event types appear in `getEventLog()` at their triggering points",
			"tests": ["ROOM-EVENTLOG-001", "ROOM-EVENTLOG-002", "ROOM-EVENTLOG-005", "ROOM-EVENTLOG-006", "ROOM-EVENTLOG-007", "ROOM-EVENTLOG-008", "ROOM-EVENTLOG-009", "ROOM-EVENTLOG-010", "ROOM-EVENTLOG-011", "ROOM-EVENTLOG-023", "ROOM-EVENTLOG-024"]
		},
		"eventlog-flat-shape-and-missing-action-events": {
			"actual": "xxscreeps's event-log emission either uses a flat shape (HARVEST, ATTACK_RANGED, HEAL_MELEE put fields directly on the event instead of nested under `data`) or omits the event entirely (BUILD, REPAIR, RANGED_MASS damage entries, HIT_BACK, DISMANTLE, creep rangedHeal RANGED)",
			"expected": "All event-log entries match vanilla shape (`{event, objectId, data: {...}}`) and BUILD/REPAIR/RANGED_MASS/HIT_BACK/DISMANTLE/rangedHeal are emitted at their triggering points",
			"tests": ["ROOM-EVENTLOG-012", "ROOM-EVENTLOG-013", "ROOM-EVENTLOG-014", "ROOM-EVENTLOG-015", "ROOM-EVENTLOG-016", "ROOM-EVENTLOG-017", "ROOM-EVENTLOG-018", "ROOM-EVENTLOG-021", "ROOM-EVENTLOG-022", "ROOM-EVENTLOG-025"]
		},
		"withdraw-enemy-rampart-no-protection": {
			"actual": "`withdraw()` does not enforce `ERR_NOT_OWNER` for hostile structures under a non-public enemy rampart",
			"expected": "Returns `ERR_NOT_OWNER` when the target sits under a non-public hostile rampart",
			"tests": ["WITHDRAW-005"]
		},
		"controller-my-never-owned-returns-false": {
			"actual": "`StructureController.my` returns `false` on a never-owned controller (`#user` undefined)",
			"expected": "Returns `undefined` on a never-owned controller (vanilla `OwnedStructure.my` maps `_.isUndefined(o.user) ? undefined : ...`, reserving `false` for `user === null` after unclaim/downgrade)",
			"tests": ["CTRL-CLAIM-007"]
		},
		"moveto-nopathfinding-returns-ok": {
			"actual": "`moveTo({noPathFinding: true})` returns `OK` when no cached path exists",
			"expected": "Returns `ERR_NOT_FOUND` when no reusable path is available",
			"tests": ["MOVE-BASIC-019"]
		},
		"pull-spawning-no-guard": {
			"actual": "`pull()` on a spawning creep returns `OK` (no spawning check in pull intent)",
			"expected": "Returns `ERR_INVALID_TARGET` when the target is a spawning creep",
			"tests": ["MOVE-PULL-007:spawning"]
		},
		"active-bodyparts-takewhile-front-damage": {
			"actual": "`getActiveBodyparts(type)` returns 0 once any earlier-indexed body part is dead. New helper `iterateActiveParts` (introduced in `mods/creep/creep.ts:450` by upstream commit 301685e) does `Fn.takeWhile(body, p => p.hits > 0)`, halting at the first dead part. But `recalculateBody` (`mods/creep/processor.ts:80-86`) damages parts left-to-right, so a partially-damaged creep has dead parts at the front and live parts behind them — `takeWhile` skips the live ones.",
			"expected": "`getActiveBodyparts(type)` counts every still-alive part of `type` regardless of dead parts at lower indices. Vanilla canonical: damage is distributed front-first, but the active-part count remains a true filter, not a prefix scan.",
			"tests": ["MOVE-FATIGUE-007"]
		},
		"transfer-controller-no-upgrade-redirect": {
			"actual": "`transfer(controller, RESOURCE_ENERGY)` returns `ERR_NOT_IN_RANGE` and does not redirect",
			"expected": "Redirects to `upgradeController` behavior (or equivalent successful upgrade intent)",
			"tests": ["TRANSFER-011"]
		},
		"shape-body-part-always-has-boost": {
			"actual": "Unboosted body parts expose `boost` key (with `undefined` value)",
			"expected": "`boost` key is only present when the part is actually boosted",
			"tests": ["SHAPE-CREEP-002", "SHAPE-CREEP-003"]
		},
		"shape-flag-crash": {
			"actual": "Flag shape discovery crashes (`Cannot use 'in' operator on undefined`)",
			"expected": "Flag objects expose the documented shape without crashing",
			"tests": ["SHAPE-FLAG-001"]
		},
		"spawn-duplicate-name-allowed": {
			"actual": "`spawnCreep` allows a name that collides with a currently spawning creep (no check against spawning creeps in `checkSpawn`)",
			"expected": "Returns `ERR_NAME_EXISTS` when the name collides with a spawning creep",
			"tests": ["SPAWN-CREATE-003"]
		},
		"rawmemory-set-no-eager-limit-check": {
			"actual": "`RawMemory.set(largeString)` returns normally; the 2MB cap throws later inside `memory/memory.ts:flush()` during `runtimeConnector.send`, surfaced to the adapter as a runtime sandbox error rather than a user-code exception",
			"expected": "`RawMemory.set` throws synchronously at call time when the value exceeds the 2MB limit, so a user-code try/catch can observe the throw",
			"tests": ["MEMORY-004"]
		},
		"rawmemory-set-invalidates-parsed-memhack": {
			"actual": "`RawMemory.set` clears the cached parsed `Memory`, so a subsequent `Memory.x` or object `.memory` access re-parses from the newly-set string and loses pre-set mutations. The underlying mechanism gap: the `Memory` global is bound as a per-access getter that does not self-replace into a value descriptor on first access (vanilla redefines `Memory` as `{ value: parsed }` after first access; observable via `UNDOC-MEMHACK-012`).",
			"expected": "Setting `RawMemory` after `Memory` or an object `.memory` accessor has been accessed preserves the already-parsed `Memory` object for the rest of the tick (memhack). The `Memory` global descriptor flips from accessor to value descriptor on first access.",
			"tests": ["MEMORY-002", "UNDOC-MEMHACK-007", "UNDOC-MEMHACK-008", "UNDOC-MEMHACK-009", "UNDOC-MEMHACK-010", "UNDOC-MEMHACK-012"]
		},
		"foreign-segment-not-supported": {
			"actual": "`setActiveForeignSegment(null)` does not clear the pending foreign-segment request — the stale request keeps `RawMemory.foreignSegment` populated on the following tick",
			"expected": "Passing `null` to `setActiveForeignSegment` clears the request so `RawMemory.foreignSegment` is `undefined` next tick",
			"tests": ["RAWMEMORY-FOREIGN-006"]
		},
		"packedpos-write-ignored": {
			"actual": "Writing to `RoomPosition.__packedPos` does not update the position's `x`, `y`, or `roomName` getters (likely stored separately from `__packedPos`)",
			"expected": "Assigning `__packedPos` updates `x`, `y`, `roomName` to the decoded values — bots use this to construct positions cheaply (including from WASM bridges)",
			"tests": ["UNDOC-PACKEDPOS-003"]
		},
		"memory-parsed-json-not-refreshed-across-ticks": {
			"actual": "xxscreeps caches the parsed-memory `json` object as module-level state (`mods/memory/memory.ts`) and does NOT re-parse raw memory at the start of each tick. Tick-end serialization correctly produces vanilla-compatible raw memory (function keys dropped, `NaN`/`Infinity` → `null` via `JSON.stringify`) but the in-memory `Memory` object on the next tick still contains the original values (the function object, `NaN`, `Infinity`) because it's the same cached `json` reference, not a fresh parse of the raw string. Same root cause for `UNDOC-MEMHACK-011`'s tick-3 `Memory.x` assertions: when a tick skips save via `delete RawMemory._parsed`, raw memory is correctly preserved, but `Memory` on the next tick still reflects the cached (mutated) object instead of a fresh parse.",
			"expected": "`Memory` on each tick reflects a fresh `JSON.parse(RawMemory.get())` — values that `JSON.stringify` coerces (functions stripped, `NaN`/`Infinity` → `null`) round-trip to those coerced forms when read on the next tick, matching vanilla's per-tick-re-parse semantics.",
			"tests": ["UNDOC-MEMJSON-001", "UNDOC-MEMJSON-003", "UNDOC-MEMJSON-004", "UNDOC-MEMHACK-011"]
		},
		"memory-circular-ref-crash": {
			"actual": "A circular reference in `Memory` causes xxscreeps's `crunch` normalizer (`mods/memory/memory.ts`) to recurse until stack overflow (`RangeError: Maximum call stack size exceeded`), crashing the player runtime. `crunch` has no cycle detection; the subsequent `JSON.stringify` would also throw, but `crunch` runs first and its throw is not caught.",
			"expected": "Circular references fail gracefully — the unserializable subtree does not persist, but the player runtime stays alive and other Memory keys that do not participate in the cycle remain readable on the next tick.",
			"tests": ["UNDOC-MEMJSON-005"]
		}
	}
}
